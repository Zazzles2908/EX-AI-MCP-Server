# === EX-AI MCP Server - Simplified Configuration (Generated) ===
# Review and copy into your .env when ready.

# === CORE PROVIDER KEYS (Required) ===
KIMI_API_KEY=
GLM_API_KEY=

# === MODEL DEFAULTS ===
DEFAULT_MODEL=glm-4.5-flash          # fast manager default to avoid 'auto' rejections in some tools; router still available when explicitly requested
KIMI_DEFAULT_MODEL=kimi-k2-0905-preview # router hint for Kimi
GLM_DEFAULT_MODEL=glm-4.5-flash        # router hint: flash
GLM_QUALITY_MODEL=glm-4.5
KIMI_QUALITY_MODEL=kimi-k2-0905-preview

# === CORE FEATURES ===
ENABLE_AI_MANAGER=true
ENABLE_WEB_SEARCH=true
ENABLE_FILE_UPLOAD=true
ENABLE_FALLBACK=true

# === TIMEOUTS (Seconds) ===
TOOL_TIMEOUT=300
PROVIDER_TIMEOUT=50
UPLOAD_TIMEOUT=180

# === SERVER CONFIG ===
MCP_SERVER_NAME=exai
EXAI_WS_HOST=127.0.0.1
EXAI_WS_PORT=8765
LOG_LEVEL=INFO

# === OPTIONAL PROVIDERS ===
OPENROUTER_API_KEY=
CUSTOM_API_URL=

# === ADVANCED (Optional) ===
MAX_FILE_SIZE_MB=100
MAX_CONTEXT_TOKENS=128000
ENABLE_CACHING=true
CACHE_TTL_HOURS=24

# === Moonshot (Kimi) OpenAI-compatible base_url ===
OPENAI_BASE_URL=https://api.moonshot.ai/v1

