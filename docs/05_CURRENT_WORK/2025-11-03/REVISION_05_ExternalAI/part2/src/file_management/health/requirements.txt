"""
Requirements for File Health Check System

Install the required packages using:
pip install -r requirements.txt
"""

# Core dependencies
aiofiles>=23.0.0          # Async file operations
aiohttp>=3.8.0            # Async HTTP client
psutil>=5.9.0             # System and process utilities

# Optional dependencies (for enhanced functionality)
# supabase>=1.0.0        # Supabase client (for cloud storage)
# pydantic>=2.0.0        # Data validation (if enhanced validation needed)

# For systems without pwd/grp modules (Windows)
# No additional dependencies needed - built-in fallback implemented